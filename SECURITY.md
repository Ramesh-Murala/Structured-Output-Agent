# Security Policy

Do not include API keys, credentials, personal data, or production secrets in issues or commits.

For real deployments, redact sensitive model inputs and invalid outputs before writing validation logs. Add authentication, authorization, request limits, rate limiting, TLS termination, and centralized secret management before exposing the service publicly.
